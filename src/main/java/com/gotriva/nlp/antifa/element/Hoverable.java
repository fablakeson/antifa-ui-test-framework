package com.gotriva.nlp.antifa.element;

/** An interface to represent a mouse-hover interaction capable element. */
public interface Hoverable extends Interactable {

  /** Perform a mouse hover on this element. */
  void hoverOn();
}
